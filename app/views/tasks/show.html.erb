<h1><%= @task.title %></h1>
<p>by <%= link_to @task.user.name, user_path(@task.user) if @task.user %> (<%= link_to "Edit Task", edit_user_task_path(@task.user, @task) if @task.user %>)</p>
<p><%= @task.description %> </p>